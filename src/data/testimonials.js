/**
 * ---------------------------------------------------------------------------
 * TESTIMONIALS
 * ---------------------------------------------------------------------------
 * Intentionally empty. Do not add a testimonial unless a real client actually
 * said it - the section renders an honest "coming soon" state while this list
 * is empty, and switches to a carousel of quotes as soon as you add one.
 *
 * Shape:
 *   quote    string   What the client said, in their words
 *   name     string   Client name (with their permission)
 *   role     string   Role and/or company
 *   avatar   string   Optional image path; initials are shown when omitted
 *   project  string   Optional - the project they are talking about
 * ---------------------------------------------------------------------------
 */

export const testimonials = []
